# API server port (default: 3000)
API_PORT=3000

# UI server port (default: 5173)
UI_PORT=5173

# CORS allowed origin (optional; defaults to permissive if unset)
# CORS_ALLOWED_ORIGIN=http://localhost:5173
