# CVE allowlist for the Java dependency audit (trivy sbom over the CycloneDX SBOM,
# run via `./arcogine check --full` or `cd product && ./gradlew cyclonedxBom`).
#
# Only CVEs in dependencies that are NOT shipped in a running deployable are
# listed here — they come from test-only or build-only configurations and do
# not affect retained code. Shipped-runtime CVEs are deliberately NOT
# suppressed and will fail the gate.

# plexus-utils — build-time tooling (Gradle/CycloneDX machinery). Not shipped.
CVE-2025-67030
