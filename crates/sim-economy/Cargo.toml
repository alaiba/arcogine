[package]
name = "sim-economy"
version = "0.1.0"
edition = "2021"

[dependencies]
sim-types = { path = "../sim-types" }
sim-core = { path = "../sim-core" }
serde = { version = "1", features = ["derive"] }
rand = "0.9"
rand_chacha = "0.10"
