# Events–State–Observations Architecture Assessment

> **Date:** 2026-08-20
> **Scope:** Formalize the Events–State–Observations philosophy as a first-class architectural principle, assess the current Java implementation against it, and produce a staged backlog. This document does not implement the refactor.

## Canonical invariant

```text
Events mutate State.
State produces Observations.
Observations inform Decisions.
Decisions produce Events.
```

See `docs/architecture.md` (section "Core Architecture Philosophy: Events, State, Observations") for the full narrative treatment of Events, State, Observations, and Decisions, and for the list of signals that should trigger architectural review.

## Update 2026-08-20 — Pricing/order semantics resolved

A follow-up product clarification resolves the "domain decision required" flagged below for revenue timing, and **overrides that section's conclusion**. It also refines the code fix already merged for the `SnapshotBuilder` bug (see below). This update does not replace the rest of the assessment — the architecture matrix (B) and backlog (C) are amended in place; everything not touched here still stands.

**Resolution.** `price` was never one concept. Arcogine distinguishes:

| Concept | Meaning | Owner / location | Mutability |
|---|---|---|---|
| **OfferPrice** | The simulated firm's current asking price; input to the demand model. (Terminology corrected 2026-08-20 (3) — see below; was called "MarketPrice" in this section originally, which overloaded the term with a future external-market concept.) | `PricingState` | Mutable, changes on `PriceChange` |
| **OrderPrice** (unit price) | Price agreed when a specific order was created. | Captured on `OrderCreation`, carried by the order/job | Immutable once the order exists |
| **OrderValue** | `quantity x OrderPrice` for one order. | Derived from the order | Derived |
| **CompletedSalesValue** | Sum of `OrderValue` for completed orders. | Factory/operational KPI | Accumulates using each order's own `OrderPrice` |
| Revenue | Reserved for a future finance/accounting domain (recognition policy, receivables, deferred revenue, etc.) | Not modeled | — |

The previously open question — "does revenue use price-at-order-creation or price-at-completion?" — is **resolved as price-at-order-creation**: `OrderPrice` is captured once, at `OrderCreation`, and is immutable for the life of the order. `OfferPrice` changes only affect orders created *after* the change. This is a product decision about commercial transaction semantics, not the introduction of an accounting model — see `docs/architecture.md`'s "Pricing, orders, and money" section and `docs/concepts.md`'s "Completed sales value" section for the full narrative and the explicit non-goal (no `RevenueRecognitionPolicy`, `AccountsReceivable`, `PaymentTerms`, `CashReceipt`, `AccruedRevenue`, or `DeferredRevenue`).

**This changes the classification of code inspected in the original review:**

- `PricingState.currentPrice` → this **is** `OfferPrice`. Correctly named conceptually (if not literally), correctly the sole event-driven owner. No change to its role.
- `DemandModel.currentPrice` → also `OfferPrice` (a copy). `DemandModel` correctly uses it only to compute demand and only for orders not yet created — using `OfferPrice` here is correct by construction; the problem was always the duplicate mutable copy, not the semantic.
- `EventPayload.OrderCreation(ProductId productId, long quantity)` → **gap**. It captures `quantity` but not `OrderPrice`, even though the product decision requires the order's unit price to be fixed at exactly this event. This is the central structural gap the new backlog item below closes.
- `FactoryHandler.currentPrice` (set via `setCurrentPrice`, pushed from `PricingState` by `IntegratedHandler` every event) → **misclassified concept**, not just duplicated state. The factory has no legitimate reason to know `OfferPrice` at all under the resolved semantics — it should only ever need each order's own `OrderPrice`, captured once at creation. This is stronger than the original finding ("duplicated state, sync it or read on demand") — the correct fix is to **delete** `FactoryHandler.currentPrice`/`setCurrentPrice` entirely, not to have `FactoryHandler` read `PricingState` on demand.
- `FactoryHandler.totalRevenue` / `handleTaskEnd`'s `totalRevenue += currentPrice * job.quantity()` → this **is** `CompletedSalesValue` conceptually, but computed from the wrong input (`OfferPrice`/`currentPrice` at completion time) instead of the right one (each order's own `OrderPrice`, captured at creation). Needs both a rename (to `completedSalesValue`) and a computation fix (source the price from the order, not from live pricing state).
- `Job` → currently has no field for its own agreed price. This needs to change (see amended backlog item below) — this is where `OrderPrice`/`OrderValue` belong.
- `AgentObservation.currentPrice` / `AgentObservation.totalRevenue` → these **are** `OfferPrice` and `CompletedSalesValue` respectively; rename for precision, no semantic change — the agent already only ever reads `currentPrice` to decide a *new* `OfferPrice` (`SalesAgent.decide()`), never to touch any order's terms, so `SalesAgent`'s behavior already matches the resolved capability boundary ("agent observes/decides OfferPrice, never reprices existing orders"). This should be asserted by a test (see amended backlog), not just be true by absence of code that would violate it.
- `FactoryHandler.completedSales` → count of completed orders/jobs, distinct from `completedSalesValue` (monetary). Already distinct in the code (two separate fields); just confirm naming doesn't conflate them once `totalRevenue` is renamed.

**This changes the classification of the fix already merged for the `SnapshotBuilder` bug** (commit `e5ca23b`, "fix(revenue): fix per-job revenue in API snapshots to use completion price"). That fix correctly closed the *inconsistency* between `SnapshotBuilder`'s per-job number and `FactoryHandler.totalRevenue` (both now agree with each other) — but it fixed it by capturing `Job.revenue` at **task-end/completion time** (`FactoryHandler.currentPrice` as of `handleTaskEnd`), which is now known to be the wrong semantic per the resolved decision. It should have captured the price at **order-creation time** instead. The bug it fixed (aggregate vs. per-job disagreement) stays fixed either way; what changes is *which* price both numbers should agree on. This is now the first item in the amended backlog (P0, superseding the "SnapshotBuilder per-job revenue" backlog item below) — since it directly continues that just-shipped change, it is small, well-scoped, and covered by the same tests, it can reasonably be picked up next rather than deferred, but it is still a deliberate follow-up rather than something to silently fold into this document.

**Coupling implication (was Task 6 in the original review, `PricingState → FactoryHandler` sync).** Under the resolved semantics, the factory never needs `OfferPrice` — it only needs each order's own `OrderPrice`, which arrives already attached to `OrderCreation`. So `IntegratedHandler.factory.setCurrentPrice(pricing.currentPrice())` and `FactoryHandler.currentPrice`/`setCurrentPrice` should be deleted outright, not merely refactored into a read-on-demand interface. This is a stronger, simpler outcome than the original assessment's P1 item ("remove pushed copies, read on demand") for this specific edge — confirmed by the implementation, not merely suspected. The `DemandModel.currentPrice` duplication (still legitimately `OfferPrice`, still duplicated across `PricingState`/`DemandModel`) is unaffected by this update and still needs the P1 "read on demand" treatment from the original backlog.

See the amended matrix (B) and backlog (C) rows below for the concrete follow-up. Rows not mentioned there are unaffected by this update.

## Update 2026-08-20 (2) — Finance established as a domain, now

A second follow-up refines the previous position on revenue/accounting. The earlier statement —

> Revenue is reserved for a future finance/accounting domain and is not currently modeled.

— is replaced with:

> Arcogine establishes a minimal Finance domain now, with a deliberately minimal double-entry ledger, so financial interpretation has a clear owner before monetary concepts leak into Factory or Economy the way `currentPrice` once leaked into `FactoryHandler`. Accounting *sophistication* (recognition policy, receivables, tax, multi-currency) remains future scope — the domain boundary does not.

This is a scope decision, not an implementation in this pass — `sim-finance` does not exist yet. What changes here is the target architecture and backlog; see `docs/architecture.md`'s new "Commercial, Operational, and Financial Truth" section for the full narrative (three kinds of truth — commercial/operational/financial — related by events, never interchangeable; the initial immediate-settlement policy; why Finance now rather than later; the `OrderCompleted` event; the minimal double-entry mechanism; the money-representation boundary; the ownership table; agent/observation capability boundaries).

**Three kinds of truth, and who owns each** (full table in `docs/architecture.md`):

| Concept | Owner |
|---|---|
| `ObservedMarketPrice` (external market signal) | Future environment/market domain; not currently required |
| `OfferPrice`, demand state | Economy (`PricingState`, `DemandModel`) |
| `OrderPrice`, `OrderValue` | Immutable commercial order data (`Job`) |
| Production state, order completion (operational fact) | Factory (`FactoryHandler`) |
| Backlog / throughput / lead time | Factory, or a KPI/projection layer |
| Ledger, cash, sales financial balance, future receivables/payables | Finance (**new**, not yet implemented) |

**Reassessing `CompletedSalesValue` (Task 7 of the follow-up prompt).** The prior pass established `FactoryHandler.completedSalesValue` as the replacement for `totalRevenue`. That naming and computation are still correct as an **operational/commercial KPI** ("what commercial value has completed production?") — but it must not be confused with a future Finance-owned "Sales" balance ("what has been financially recorded under the active policy?"). Under the initial immediate-settlement policy these numbers will coincide, but they are not the same concept and are not guaranteed to stay equal once Finance's policy gains payment terms or other timing effects. `CompletedSalesValue` stays where it is (Factory, computed from completed orders' own `OrderValue`) — moving it into a KPI/projection layer is not required by this decision, since it's already a simple derived sum over authoritative operational facts (`Job.orderValue()`), not a second mutable accumulator; revisit only if a real second consumer of the same derivation appears.

**Event vocabulary gap.** `TaskEnd` currently means "a production step finished" — nothing today expresses "the order fulfilled its operational lifecycle" as a first-class fact. `FactoryHandler.handleTaskEnd` checks `job.isComplete()` internally but does not emit anything downstream-observable for that specific fact; `completedSalesValue`/`completedSales` are updated in-place, not announced. A Finance domain needs the latter, named fact, not an inference from the former. See the new `OrderCompleted` backlog item below.

**Non-goal refinement (Task 13).** Section D's "No accounting/finance model" bullet is refined in place below to "No sophisticated accounting model" — the minimal double-entry ledger is intentional current architecture, not a non-goal.

See the amended matrix (B) and backlog (C) rows below.

## Update 2026-08-20 (3) — Pricing vocabulary corrected: `OfferPrice`, not `MarketPrice`

A terminology-only amendment, not a scope change: everywhere the prior updates used **`MarketPrice`** for the firm's own mutable asking price, that term is corrected to **`OfferPrice`**. This document, `docs/architecture.md`, `docs/concepts.md`, and `CONTRIBUTING.md` have all been updated in place (the historical narrative above — "Update 2026-08-20" and "Update 2026-08-20 (2)" — keeps its prose as originally written except for this terminology swap; no conclusions changed).

The reason: "market price" implies an external/environmental signal (what the broader market says a product is worth), which Arcogine does not model. What Arcogine actually has is the firm's own controllable asking price. Conflating the two would misname a future concept before it exists. The corrected vocabulary:

| Concept | Meaning | Status |
|---|---|---|
| `ObservedMarketPrice` | External/environmental market signal | **Not implemented** — reserved for a future environment/market domain |
| `OfferPrice` | The firm's current asking price (was called `MarketPrice` above) | Implemented — `PricingState.currentPrice` |
| `OrderPrice` | Price agreed for an accepted order | Implemented — `Job.unitPrice` |
| `OrderValue` | `quantity x OrderPrice` | Implemented — `Job.orderValue()` |

The invariant: **the environment may inform `OfferPrice`; the firm controls `OfferPrice`; accepting an order freezes that price into `OrderPrice`; Finance later consumes the resulting immutable commercial facts.** `ObservedMarketPrice` does not need to be implemented now — there is no external market/environment model in Arcogine — and this amendment does not introduce settlement pricing, indexed contracts, rebates, or discounts. `OrderPrice = OfferPrice at OrderCreation` remains the entire rule.

Two source-level Java comments/test names used "MarketPrice" in prose (not as an identifier — no Java field, class, or method was ever named `MarketPrice`) and have been corrected to match: `EventPayload.OrderCreation`'s Javadoc, and a test method name in `SnapshotBuilderTest`/prose in `SnapshotBuilderTest`/`FactoryHandlerTest`. These are comment/name-only changes with no behavioral difference — not a violation of the "documentation and backlog only" scope for this series of updates.

## A. Documentation changes

| File | Section | Why |
|---|---|---|
| [docs/architecture.md](../docs/architecture.md) | New: "Core Architecture Philosophy: Events, State, Observations" | Makes the philosophy explicit and authoritative, with the invariant, per-concept rules, and review triggers, per Task 1. |
| [docs/architecture.md](../docs/architecture.md) | "Event Dispatch Architecture" | Reframed to describe `IntegratedHandler`'s cross-handler sync as a named, transitional pattern rather than the target design — otherwise the doc would silently endorse the coupling this assessment flags. |
| [docs/concepts.md](../docs/concepts.md) | "Revenue" (original pass) | Documented the then-current, tested behavior (price-at-completion) and flagged it as an undecided domain question — see Task 3 (original). Superseded by the next row. |
| [docs/README.md](../docs/README.md) | "Internal planning (maintainers)" | Adds this assessment to the doc index so it's discoverable. |
| [CONTRIBUTING.md](../CONTRIBUTING.md) | New: "Architecture guardrails (Events, State, Observations)" | Gives contributors a checklist to apply during review (Task 5), with a pointer back to this document. |
| [docs/architecture.md](../docs/architecture.md) | New: "Pricing, orders, and money: OfferPrice vs. OrderPrice" (2nd pass) | Resolves the price-timing question with the OfferPrice/OrderPrice/OrderValue/CompletedSalesValue distinction, the lifecycle diagram, and the explicit non-goal (no accounting model) — per the 2nd pass's Tasks 1-4, 11-12. |
| [docs/concepts.md](../docs/concepts.md) | "Price and demand" / "Completed sales value" (replaces "Revenue", 2nd pass) | Rewrites the user-facing explanation to match the resolved semantics: offer price affects only future orders; each order locks its price at creation; the dashboard's completed-sales figure sums each order's own locked-in price. Also updates the job-lifecycle description ("Completed" step). |
| [docs/architecture.md](../docs/architecture.md) | New: "Commercial, Operational, and Financial Truth: the Finance Domain" (3rd pass) | Establishes the Finance domain decision: the three-truths model, why Finance now vs. later, Finance under Events–State–Observations, the `OrderCompleted` event, the minimal double-entry ledger, the money-representation boundary (`double` vs. `BigDecimal`), the ownership table, and the refined non-goal ("no sophisticated accounting model," not "no finance domain") — per the 3rd pass's Tasks 1-3, 5-11, 13, 16-17. Also updates "Module Structure" (planned `sim-finance`), the DES event list (planned `OrderCompleted`), and "What should trigger architectural review" (Finance-specific triggers). |
| [docs/concepts.md](../docs/concepts.md) | "Completed sales value" (3rd pass) | Distinguishes the operational KPI from a future Finance-owned "Sales" balance, without implying they'll always match. |
| [CONTRIBUTING.md](../CONTRIBUTING.md) | Guardrail 12 (new), guardrail 11 (refined) (3rd pass) | Adds the Finance domain-boundary rule (operational domains emit facts, Finance interprets them; unbalanced journal entries can't enter financial state) and removes the now-stale "Arcogine does not model accounting semantics" framing from guardrail 11. |
| [docs/architecture.md](../docs/architecture.md), [docs/concepts.md](../docs/concepts.md), [CONTRIBUTING.md](../CONTRIBUTING.md), this document (4th pass) | Terminology correction throughout: `MarketPrice` → `OfferPrice`; `ObservedMarketPrice` introduced (reserved, not implemented) | Per Task "Pricing terminology" of the 4th-pass amendment — `MarketPrice` overloaded the firm's controllable asking price with a future external-market signal Arcogine doesn't model. No architectural conclusion changes, only naming. Also corrects two Java comment/test-name-only mentions (`EventPayload.OrderCreation` Javadoc, `SnapshotBuilderTest`/`FactoryHandlerTest` prose and one test method name) — no behavior change. |

No source code was changed as part of the original (1st) pass. The 2nd pass included one already-merged code change from the 1st pass's backlog (the `SnapshotBuilder` per-job revenue fix, commit `e5ca23b`), later fully implemented as the `OrderPrice`-at-creation fix (commit `90635d5`) — see "Update 2026-08-20" above. The 3rd pass (Finance domain) is documentation and backlog only, no source changes — `sim-finance` does not exist yet. The 4th pass (this terminology amendment) touches only comments/test names in already-existing test/source files, per the explicit instruction not to expand implementation scope — see non-goals (D) and the backlog (C) below for what's deliberately deferred.

## B. Current-state architecture matrix

| Area | Status | Evidence | Risk | Recommended action |
|---|---|---|---|---|
| `Event` / `EventPayload` (sim-core) | **Aligned** | Both are Java records; `EventPayload` is a `sealed interface` with 8 record permits; `Event.of()` derives `EventType` via exhaustive pattern match. Fully immutable, inspectable, replayable via `EventLog`. | Low | None. Use as the template for future event types. |
| `EventHandler` contract (sim-core) | **Aligned** | Single-method functional interface; all domain handlers (`PricingState`, `DemandModel`, `FactoryHandler`, `SalesAgent`, `IntegratedHandler`) implement it uniformly and only mutate their own state in response to events. | Low | None. |
| `Scheduler` same-tick ordering (sim-core) | **Needs design decision** | `PriorityQueue<Event>` ordered only by `Comparator.comparing(Event::time)` — no tie-breaker for equal-`SimTime` events. `FactoryHandler.tryDispatchFromQueue` schedules `TaskStart`/`TaskEnd` at the same tick in some paths, so same-tick collisions are real, not hypothetical. | Medium — silently non-deterministic ordering among equal-time events would violate the documented determinism contract. | Decide and document an explicit tie-break rule (e.g. FIFO by insertion sequence number, or an explicit priority per `EventType`), then add a determinism test asserting stable same-tick ordering across runs. |
| `PricingState` (sim-economy) | **Mostly aligned (improved 2026-08-20)** | Owns `OfferPrice` (`currentPrice` field) + `priceHistory`, mutated only via its own `handleEvent`/`setPrice` — good ownership in isolation. `FactoryHandler`'s copy was deleted; `DemandModel` still keeps a **separate mutable copy** (legitimately needed — see its row above), so `PricingState` is not yet the *sole* representation, but the duplication is down from three copies to two. | Low–Medium — one remaining copy, kept in sync only by `IntegratedHandler`'s per-event push. | See remaining P1 backlog item on `DemandModel.currentPrice`. |
| `DemandModel` (sim-economy) | **Transitional** | `currentPrice`/`avgLeadTime` are mutable fields pushed in unconditionally by `IntegratedHandler` on *every* event (`IntegratedHandler.java:38-39`) **and** independently updated by `DemandModel`'s own `PriceChange` case (`DemandModel.java:79`) — two update paths for the same field. `generateOrders()` also schedules `OrderCreation` events directly, mixing "state" and "decision" responsibilities in one class. | Medium — redundant-but-currently-consistent sync paths are exactly the kind of thing that silently diverges under a future edit. | Pick one update path (event-driven only); stop the unconditional per-event push from `IntegratedHandler`. |
| `FactoryHandler` (sim-factory) | **Aligned on price (fixed 2026-08-20); still has an encapsulation gap** | Owns machines/jobs/queues/routing (good — single owner, mutated only via its own `handleEvent`). It no longer holds any price field at all — `currentPrice`/`setCurrentPrice` were deleted; each order's price lives on the `Job` it belongs to, captured immutably at creation. `completedSalesValue`/`completedSales` are still **public mutable fields**, not encapsulated. | Low — the price-duplication risk is gone; the remaining public-field issue is cosmetic (nothing currently writes through it). | Encapsulate `completedSalesValue`/`completedSales` behind accessors (still open, P1 below). |
| `CompletedSalesValue` computation on job completion (`FactoryHandler.handleTaskEnd`) — **RESOLVED AND IMPLEMENTED, was "Revenue computation on job completion"** | **Aligned** | `handleTaskEnd` now does `completedSalesValue += job.orderValue()`, where `job.orderValue()` is `quantity * unitPrice` and `unitPrice` was captured immutably when the `Job` was constructed from the `OrderCreation` event that created it. `FactoryHandler` no longer reads any live pricing state at all. | Low | Done — see `FactoryHandlerTest.completedSalesValueUsesOrderCreationPrice`, `jobOrderValueIsFixedAtOrderCreationPriceRegardlessOfLaterOrders`, `completedSalesValueSumsEachOrdersOwnCreationTimePrice`. |
| `SnapshotBuilder` per-job revenue (sim-api) — **RESOLVED AND IMPLEMENTED** | **Aligned** | `JobInfo.revenue` for completed jobs now reads `j.orderValue()` (derived from the job's own immutable `unitPrice`), not `pricing.currentPrice()`. Verified consistent with `FactoryHandler.completedSalesValue` by `SnapshotBuilderTest.completedJobRevenueInSnapshotMatchesOrderCreationPriceNotLaterOfferPrice`, which explicitly changes offer price between order creation and completion and asserts the snapshot is unaffected. | Low | Done. |
| `IntegratedHandler` (sim-api) | **Transitional (orchestration) / Misaligned (observation construction)** | Explicit fixed ordering (Pricing → Demand → Factory → Agent) is a reasonable, deterministic composition strategy in isolation. It performs 2 remaining cross-domain setter pushes every event (`demand.setPrice`, `demand.setAvgLeadTime` — the `factory.setCurrentPrice` push was deleted 2026-08-20) and hand-assembles `AgentObservation` from raw fields/methods on `FactoryHandler`/`PricingState` — orchestration and observation-construction are mixed into one class. | Medium — smaller than before (one fewer push), but still the "god orchestrator" risk for `DemandModel`'s remaining duplication and for observation construction. | See P1 backlog: extract observation construction to a dedicated, testable projector; keep ordering explicit but stop pushing `DemandModel`'s state copies too. |
| `HandlerFactory` (sim-api) | **Aligned (wiring); remaining `DemandModel` duplication unaffected** | Cleanly constructs each handler from `ScenarioConfig` in dependency order — no issue with wiring itself. It still seeds `initialPrice` into both `DemandModel` and `PricingState` independently (two separate fields, same source value) — that duplication is `DemandModel`'s, not `FactoryHandler`'s, and is unaffected by the 2026-08-20 fix (it never touched `FactoryHandler.currentPrice`, which no longer exists to seed). | Low | Fix alongside the remaining `DemandModel` P1 item below; not a standalone risk. |
| `AgentObservation` (sim-agents) | **Aligned structurally / naming now imprecise** | Immutable record (`backlog`, `avgLeadTime`, `totalRevenue`, `completedSales`, `currentPrice`, `throughput`), plus a `DEFAULT` singleton. Purpose-specific, matches the Observation contract structurally. Per the resolved semantics, `currentPrice` **is** `OfferPrice` and `totalRevenue` **is** `CompletedSalesValue` — the fields are conceptually correct (the agent should only ever see `OfferPrice`, never any order's price) but named generically. | Low | Rename `currentPrice` → `offerPrice`, `totalRevenue` → `completedSalesValue` for precision (P2, cosmetic — see amended backlog); construction site (`IntegratedHandler`) is what needs to move, not the type itself. |
| `SalesAgent` (sim-agents) | **Mostly aligned; capability boundary confirmed but untested** | `decide()` is a pure function over `AgentObservation` + config; on `AgentEvaluation` it *schedules* `PriceChange`/`AgentDecision` events rather than calling a setter — the cleanest Observation→Decision→Event example in the codebase. It only ever reads/proposes `currentPrice`/`OfferPrice`, never touches any order's price — this now matches the resolved capability boundary ("agent changes OfferPrice for future orders, never reprices existing ones") by construction, since it has no reference to any order/`Job` at all. `observation` and `interventions` are public mutable fields (agent's own state, not another domain's), a minor encapsulation gap. | Low | Add the "agent cannot exploit backlog repricing" test from the amended backlog (P0) to assert this boundary explicitly rather than relying on it being true only because no code path currently violates it. Encapsulate `observation`/`interventions` behind accessors for hygiene (P2); no behavioral change needed either way. |
| `SimThread` (sim-api) | **Mostly aligned** | All simulation mutation (`IntegratedHandler`, `Scheduler`, `EventLog`) is confined to one dedicated thread; published via `AtomicReference` safe publication (`snapshot`, `eventLog`) — correct given single-writer confinement, and good for determinism. | Low | None structurally. |
| `SimCommand.ToggleAgent` handling (sim-api, `SimThread`) | **Misaligned** | `ChangePrice`/`ChangeMachine` commands are correctly wrapped as `Event`/`EventPayload` and routed through `handler.handleEvent(...)`. `ToggleAgent` instead calls `handler.setAgentEnabled(...)` directly — a state mutation that bypasses the event system entirely, the one command handler that doesn't follow the Decision→Event pattern. | Low–Medium — currently harmless (agent-enabled is orchestration config, not domain state), but it's an inconsistent precedent for the next command that gets added. | Either model `AgentEnabled` as a proper event/state transition (if it should be replayable/loggable) or explicitly document it as configuration rather than simulation state, so the exception is deliberate, not accidental. |
| Duplicate wiring logic in tests (sim-api) | **Needs design decision / test-debt** | `AgentIntegrationTest.TestHandler` re-implements `IntegratedHandler.handleEvent`'s sync logic (price push, lead-time push, `AgentObservation` construction) locally instead of exercising the real `IntegratedHandler`. Can silently drift from production behavior. | Medium | Replace `TestHandler` with the real `IntegratedHandler` in that test, or delete the duplicate if it exists to test something `IntegratedHandler` can't. |
| Observation vs. API snapshot duality | **Needs design decision** | `AgentObservation` (agent-facing) and `SnapshotBuilder`'s DTO tree (`SimSnapshot`/`JobInfo`/`MachineInfo`, API/UI-facing) are two independently constructed, non-shared projections over the same underlying state, with no shared base or documented relationship. | Medium | Not necessarily wrong (different consumers legitimately need different shapes) — but should be a **documented** decision, not an emergent fact, especially given the revenue divergence above. |
| Order-completion event vocabulary (sim-factory) — **RESOLVED AND IMPLEMENTED** | **Aligned** | `FactoryHandler.handleTaskEnd` now schedules `EventPayload.OrderCompleted` (carrying `orderId`/`productId`/`quantity`/`unitPrice`) alongside its in-place `completedSalesValue`/`completedSales` update, whenever `job.isComplete()`. | Low | Done — see the `OrderCompleted` backlog row. |
| Finance domain (`sim-finance`) — **RESOLVED AND IMPLEMENTED** | **Aligned** | `sim-finance` module exists: `Account`/`Posting`/`JournalEntry`/`Ledger`/`FinanceObservation` (ledger package) and `FinanceHandler` (process package), wired into `IntegratedHandler`/`HeadlessHandler`. `FactoryHandler.completedSalesValue` remains the operational/commercial KPI; `FinanceHandler`'s `Ledger` is now the financial record, kept separate as designed. | Low | Done — see the "minimal Finance domain" backlog rows below for what shipped. |

## C. Refactoring backlog

Priorities: **P0** semantic correctness / architectural ambiguity · **P1** near-term structural improvement · **P2** scalability preparation · **P3** future guardrail.

| Priority | Task | Why | Scope | Dependencies | Risk |
|---|---|---|---|---|---|
| ~~P0~~ **DONE** | ~~Decide and document the revenue-timing business rule: price-at-order-creation vs. price-at-completion.~~ | **Resolved 2026-08-20**: price-at-order-creation. `OrderPrice` is captured once, immutably, at `OrderCreation`; `OfferPrice` changes only affect orders created afterward. Documented in `docs/architecture.md` and `docs/concepts.md`. | — | — | — |
| ~~P0~~ **DONE (superseded implementation)** | ~~Fix `SnapshotBuilder`'s per-job `JobInfo.revenue`~~ | Merged as commit `e5ca23b`: `Job` now records its own revenue at completion and `SnapshotBuilder` reads it, so the aggregate/per-job inconsistency is gone. Superseded by the next row: it captured price at completion, not creation, per the resolution above. | — | — | — |
| ~~P0~~ **DONE** | ~~Carry `OrderPrice` on `OrderCreation`, capture it on `Job` at order-creation time, and compute `CompletedSalesValue` from each order's own price at completion.~~ | **Implemented 2026-08-20.** `EventPayload.OrderCreation` gained `double unitPrice`; `DemandModel.generateOrders()` passes its current `OfferPrice` when scheduling each order; `Job` gained an immutable `unitPrice` field (constructor parameter, no setter) and a derived `orderValue()`; `FactoryHandler.handleOrderCreation` passes the order's price into `JobStore.createJob`/`Job`; `handleTaskEnd` now sums `completedSalesValue += job.orderValue()` instead of reading any price at completion; `FactoryHandler.totalRevenue` renamed to `completedSalesValue` throughout (`FactoryHandler`, `IntegratedHandler`, `HeadlessHandler`, `SnapshotBuilder`, `ArcogineCommand`, and all referencing tests). Verified via Docker (`gradle:9-jdk25`): 666 tests pass, Checkstyle clean. | — | — | — |
| ~~P0~~ **DONE** | ~~Delete `FactoryHandler.currentPrice`/`setCurrentPrice` and the `IntegratedHandler` push entirely.~~ | **Implemented 2026-08-20** alongside the item above — `FactoryHandler.currentPrice`/`setCurrentPrice` removed outright (not converted to a read path); `IntegratedHandler.factory.setCurrentPrice(pricing.currentPrice())` deleted; the equivalent line in `HeadlessHandler` (sim-cli, a second hand-written copy of the same wiring) and `AgentIntegrationTest.TestHandler` (a third copy, test-only) were updated the same way, since all three duplicated this exact push. The factory now has no reference to `PricingState`/`OfferPrice` at all. | — | — | — |
| ~~P0~~ **DONE (partial)** | ~~(a) orders retain creation-time price; (b) completed value uses creation price despite a later `OfferPrice` change~~ | **Implemented 2026-08-20** alongside the `OrderPrice` fix: `JobRoutingTest.orderPriceIsCapturedAtCreationAndOrderValueIsQuantityTimesOrderPrice`, `FactoryHandlerTest.jobOrderValueIsFixedAtOrderCreationPriceRegardlessOfLaterOrders`, `FactoryHandlerTest.completedSalesValueSumsEachOrdersOwnCreationTimePrice`, and `SnapshotBuilderTest.completedJobRevenueInSnapshotMatchesOrderCreationPriceNotLaterOfferPrice` (the last one explicitly changes `OfferPrice` between order creation and completion). | — | — | — |
| ~~P0~~ **DONE** | ~~Add the remaining semantic-invariant tests: (c) an agent that lowers `OfferPrice` to build backlog then raises it cannot inflate the value of orders already in flight; (d) the full pricing/order/value/completion sequence remains reproducible for a fixed seed across two runs.~~ | **Implemented 2026-08-20.** (c): `AgentIntegrationTest.agentPriceInterventionsNeverAlterAlreadyCreatedOrders` runs the real overload scenario with the agent enabled, then — for every order the run generated — matches each `OrderCreation` event to its job (`JobStore` assigns ids 1..N in dispatch order, now deterministic thanks to the `Scheduler` fix above) and asserts the job's `unitPrice` still equals the `OfferPrice` recorded on its own `OrderCreation` event, regardless of how many `PriceChange`s the agent issued afterward. This is the general form of the "lowers then raises" illustrative attack — it holds for price changes in either direction, since the check is per-order against that order's own creation event, not against a specific up/down sequence. (d): `ScenarioBaselinesTest.sameScenarioAndSeedProducesIdenticalOrdersAndCompletedSalesValue` runs the same scenario+seed twice and asserts identical `completedSalesValue`, `completedSales`, `eventsProcessed`, order count, and — per job — identical `unitPrice`/`orderValue`/`status`. Verified via Docker: 42/42 `sim-api` tests pass (0 failures). **This closes the P0 list in full** — every remaining backlog item is P1 or lower. | — | — | — |
| **P2** | Rename `AgentObservation.currentPrice` → `offerPrice` and `AgentObservation.totalRevenue` → `completedSalesValue`; update `SalesAgent`, `IntegratedHandler`'s observation construction, and any tests referencing the old field names. | Cosmetic precision, not a semantic fix (the fields were already conceptually correct) — bundled at P2 rather than P0 since it's a pure rename with no behavior change, and best done together with the P1 "extract observation construction" item to avoid touching `IntegratedHandler`'s hand-built observation twice. | `AgentObservation`, `SalesAgent`, `IntegratedHandler`, `SalesAgentTest`/`SalesAgentUnitTest`, `AgentIntegrationTest`. | P1 "extract `AgentObservation` construction" item below (do the rename while that code is already being touched) | Low |
| ~~P0~~ **DONE** | ~~Add a same-tick event ordering rule to `Scheduler` and a determinism test for it.~~ | **Implemented 2026-08-20.** `Scheduler` now wraps each scheduled `Event` in a private `Entry(Event, long sequence)` and orders its internal `PriorityQueue<Entry>` by `Comparator.comparing(time).thenComparingLong(sequence)` — same-`SimTime` events dequeue in FIFO insertion order instead of `PriorityQueue`'s unspecified tie-breaking. This makes the existing `TaskStart`-before-`TaskEnd` same-tick scheduling in `FactoryHandler.tryDispatchFromQueue` deterministic where it was previously accidental, and gives the `OrderCompleted` event (added just before this) a defined relative order too. Tests: `SchedulerTest.sameTickEventsDequeueInInsertionOrder` (distinguishable same-tick events dequeue in schedule order) and `sameTickOrderingIsStableAcrossRepeatedRuns` (20 repeated runs of the same 10-event same-tick schedule, asserting identical dequeue order every time). Verified via Docker: 670 tests pass. | — | — | — |
| ~~P1~~ **DONE** | ~~Document authoritative owners for existing state explicitly~~ | **Implemented 2026-08-20.** New "State ownership table" subsection in `docs/architecture.md` (under "State"), mapping each fact to its owning class and mutating event(s), plus an explicit caveat calling out `DemandModel`'s still-pending duplication (resolved by the next row). | — | — | — |
| ~~P1~~ **DONE** | ~~Remove `DemandModel.currentPrice`/`avgLeadTime` as pushed copies; read `OfferPrice` from `PricingState` and lead time from `FactoryHandler` on demand instead.~~ | **Implemented 2026-08-20.** `DemandModel` no longer stores `currentPrice`/`avgLeadTime` fields or `setPrice`/`setAvgLeadTime` methods; its constructor now takes `DoubleSupplier offerPrice`/`DoubleSupplier avgLeadTime` and reads them live in `computeDemand()`/`generateOrders()`. `DoubleSupplier` (not a direct `FactoryHandler` reference) keeps `sim-economy` from gaining a compile-time dependency on `sim-factory` just to read lead time. `IntegratedHandler`'s per-event push (`demand.setPrice(...)`, `demand.setAvgLeadTime(...)`) is deleted; `HandlerFactory`, `HeadlessHandler` (sim-cli), and `AgentIntegrationTest` (test-only construction) now wire `DemandModel` with `pricing::currentPrice`/`factory::avgLeadTime` at construction time instead. `DemandModel.handleEvent`'s `PriceChange` case is removed — no longer relevant, since price is always read fresh. Also removes the initial `initialPrice` constructor parameter entirely (redundant now that `PricingState` is the only place that value is seeded). Verified via Docker: 673 tests pass. | — | — | — |
| ~~P1~~ **DONE** | ~~Extract `AgentObservation` construction out of `IntegratedHandler` into a small, dedicated, testable projector.~~ | **Implemented 2026-08-20.** New package-private `AgentObservationProjector` in `com.arcogine.api.state` (sim-api, not sim-agents — `sim-agents` deliberately has no compile-time dependency on `sim-factory`/`sim-economy`, and `AgentObservation` itself lives there, so the projector had to live where all three domains are already visible, which is sim-api alongside `IntegratedHandler`). `IntegratedHandler.handleEvent` now calls `AgentObservationProjector.project(factory, pricing, scheduler.currentTime().ticks())` instead of hand-building the record inline. New `AgentObservationProjectorTest` asserts observation contents against known `FactoryHandler`/`PricingState` state, independent of the full event pipeline, plus the `elapsedTicks <= 0` floor-to-1 edge case. | — | — | — |
| ~~P1~~ **DONE** | ~~Encapsulate `FactoryHandler.completedSalesValue`/`completedSales` and `SalesAgent.observation`/`interventions` behind accessors.~~ | **Implemented 2026-08-20.** Both fields on each class are now `private` with public accessor methods (`completedSalesValue()`, `completedSales()`, `observation()`, `interventions()`); all external read sites across `sim-api`, `sim-cli`, and their tests updated to the accessor form. `FactoryHandlerTest.throughputRateDivision`, which previously set `completedSales` directly to fabricate a fixture value, now drives 10 real order-to-completion cycles through the handler instead — a small behavioral strengthening as a side effect of removing the direct-write escape hatch. `SalesAgent.config` was left public (it's an immutable record reference, no mutation risk). Verified via Docker: 675 tests pass. | — | — | — |
| ~~P1~~ **DONE (superseded)** | ~~Add tests asserting `AgentObservation` contents are correct... and a test that revenue reported by `SnapshotBuilder` matches `FactoryHandler.totalRevenue`...~~ | The `SnapshotBuilder`/`FactoryHandler` agreement test landed as `SnapshotBuilderTest.completedJobRevenueInSnapshotMatchesPriceAtCompletionNotLivePrice` (commit `e5ca23b`) — but per the resolution above it needs updating for creation-time price, tracked in the P0 `OrderPrice` item. The `AgentObservation`-contents test is still open — see the P1 "extract observation construction" item below, which should include it. | — | — | — |
| ~~P1~~ **DONE** | ~~Model `SimCommand.ToggleAgent` consistently with `ChangePrice`/`ChangeMachine`.~~ | **Implemented 2026-08-20, as a proper event** (the two options were: event-driven, or documented as orchestration config — event-driven was chosen for consistency with `ChangePrice`/`ChangeMachine`, since `agentEnabled` benefits from being part of the deterministic, replayable event stream/log the same way they are). New `EventPayload.AgentEnabledChanged(boolean enabled)` (+ `EventType.AgentEnabledChanged`, `Event.of`'s exhaustive switch). `IntegratedHandler.handleEvent` mutates its own `agentEnabled` field only in response to this event; `IntegratedHandler.setAgentEnabled(boolean)` — the direct setter that bypassed the event system — is deleted. `SimThread`'s `ToggleAgent` case now constructs and dispatches the event exactly like `ChangePrice`/`ChangeMachine` (append to log, notify listeners, `handler.handleEvent`, `eventsProcessed++`, publish snapshot) instead of calling the setter directly. New `IntegratedHandlerTest` asserts the flag only changes via the event and that a disabled agent isn't dispatched on `AgentEvaluation`. Verified via Docker: 677 tests pass. **This closes the full non-Finance P1 list** — only the Finance-domain P1/P2 items and P2/P3 scalability/guardrail items remain. | — | — | — |
| ~~P1~~ **DONE** | ~~Replace `AgentIntegrationTest.TestHandler`'s duplicated wiring logic with the real `IntegratedHandler`.~~ | **Implemented 2026-08-20** alongside the `DemandModel` fix above — `TestHandler` deleted; `AgentIntegrationTest.buildHandler` now constructs and returns the real `IntegratedHandler` directly (it still builds `FactoryHandler`/`DemandModel`/`PricingState` itself, since the test needs a non-default `SalesAgentConfig` that `HandlerFactory.buildFromConfig` doesn't parameterize — but dispatch now goes through production `IntegratedHandler.handleEvent`, not a hand-copied version of it). Test methods updated from field access (`handler.factory`, `handler.agent`) to the real class's accessor methods (`handler.factory()`, `handler.agent()`). | — | — | — |
| P2 | Define an explicit state-query interface pattern for `IntegratedHandler` to use instead of reaching into concrete handler classes (e.g. narrow read-only accessor interfaces each domain handler implements), so adding a domain doesn't mean adding new direct field access from the orchestrator. | Prepares for inventory/procurement/finance/workforce without `IntegratedHandler` growing linearly with each new domain's internals. Prefer this over a generic event bus (explicitly a non-goal). | New narrow interfaces per domain (e.g. `PriceSource`, `LeadTimeSource`) + `IntegratedHandler` depends on interfaces, not concrete classes. | P1 items above (duplicate-state removal, observation extraction) should land first so this isn't designed around the old push model | Medium — real design work, should be scoped as its own follow-up, not bundled |
| P2 | Establish a documented convention for "domain-owned observation types" (each domain exposes its own `XObservation` record, analogous to `AgentObservation`) versus API/UI snapshot DTOs, and require new domains to follow it. | Guardrail for the "Later" horizon: prevents each new domain from inventing its own ad hoc projection shape, and prevents `SnapshotBuilder` from becoming the de facto second source of truth it already partially is. | Docs + a template/example; enforced via code review using the CONTRIBUTING.md guardrails already added. | State-ownership doc | Low |
| P2 | Add an architecture/package-dependency test (e.g. ArchUnit) asserting agents and cross-domain consumers never call setter-shaped methods on other domains' handler classes, and that `sim-agents` has no compile-time dependency on `sim-factory`/`sim-economy` internals beyond `AgentObservation`. | Turns the guardrails in CONTRIBUTING.md into something CI enforces, not just review discipline — directly requested as a "Now" hygiene candidate, scoped here as P2 since it depends on the P1 cleanup landing first (otherwise it fails immediately on existing code). | New Gradle test module/dependency (e.g. `com.tngtech.archunit`), one focused rule set. | P1 duplicate-state removal (otherwise the rule fails on day one) | Low–Medium |
| P3 | Document, as guardrail text (already partly done in this pass via CONTRIBUTING.md), the rule that new domains must not require pairwise setter wiring into every existing domain — and revisit the P2 state-query-interface pattern's adequacy once a real second domain (e.g. inventory) is added. | Keeps `IntegratedHandler` from becoming a god orchestrator as domains multiply; validates the P2 interface pattern against a real case instead of a hypothetical one. | Docs now; implementation deferred until a concrete new domain is scoped. | P2 state-query interfaces | Low (docs); unknown until a real domain lands |
| P3 | Revisit whether explicit fixed handler ordering in `IntegratedHandler` (Pricing → Demand → Factory → Agent) should become a declared, testable ordering contract (e.g. an ordered list with a unit test asserting the sequence) rather than implicit in method-call order, once more handlers exist. | "Event ordering becoming implicit... dependent on registration accident" is one of the named review triggers; today's ordering is simple enough to read at a glance, but that won't stay true as domains are added. | `IntegratedHandler` + a test enumerating expected order. | P2 items (this is easier once orchestration is thinner) | Low |
| ~~P1~~ **DONE** — Finance | ~~Introduce `OrderCompleted` as a first-class `EventPayload`~~ | **Implemented 2026-08-20.** `EventPayload.OrderCompleted(JobId orderId, ProductId productId, long quantity, double unitPrice)` added to the sealed permits list (and `EventType.OrderCompleted`, and `Event.of`'s exhaustive switch). `FactoryHandler.handleTaskEnd` schedules it via `scheduler.schedule(...)` at the same tick, in addition to (not instead of) its existing in-place `completedSalesValue`/`completedSales` update, only when `job.isComplete()` — confirmed not scheduled on intermediate step completions. Same-tick ordering relative to other same-tick events is *not* separately resolved here — it inherits the existing, still-open `Scheduler` same-tick P0 item above; no new determinism guarantee was added or claimed. Tests: `FactoryHandlerTest.multiStepRoutingAdvancesToNextStep` (extended), `intermediateStepDoesNotScheduleOrderCompleted`, `orderCompletedEventCarriesTheOrdersCommercialFacts`, `EventTest.eventTypeDerivedFromEachPayloadVariant` (extended). One existing test (`completedSalesValueSumsEachOrdersOwnCreationTimePrice`) had to be updated to explicitly drain the new `OrderCompleted` event between two sequential manually-driven orders — a leftover unprocessed event in the scheduler queue would otherwise have been picked up by a later `nextEvent()` call meant for a different event, which is itself a small demonstration of why the same-tick ordering item above still matters. Verified via Docker: 668 tests pass. `FinanceHandler` does not yet exist to consume this event — it currently reaches every handler's `default -> {}` no-op, same as any event a handler doesn't care about. | Should land before or alongside the Finance module below (Finance's `FinanceHandler` reacts to this event) | — |
| ~~P1~~ **DONE** — Finance | ~~Introduce a minimal `sim-finance` module.~~ | **Implemented 2026-08-20.** New Gradle module `java/sim-finance` (`com.arcogine.finance.ledger`: `Side`, `Account` — a 2-value enum, `CASH`/`SALES`, each with a `normalSide()`, deliberately not a generic String-keyed chart-of-accounts — `Posting`, `JournalEntry`, `Ledger`, `FinanceObservation`; `com.arcogine.finance.process`: `FinanceHandler`), depending only on `sim-types`/`sim-core`. `Posting`'s compact constructor rejects non-positive amounts (`SimError.OutOfRange`); `JournalEntry`'s compact constructor rejects empty postings and unbalanced entries (new `SimError.UnbalancedJournalEntry`) — there is no way to construct an unbalanced entry, let alone post one. `Ledger.post` only appends already-validated entries; `Ledger.balance(Account)` sums in that account's own normal direction. `FinanceHandler` reacts only to `OrderCompleted`, converting the event's `double` `quantity`/`unitPrice` to a `BigDecimal` (`setScale(2, HALF_UP)`) at this one boundary — the money-representation policy from `docs/architecture.md` — and posts `DR Cash / CR Sales` for that amount; every other event falls through its `default -> {}`, same broadcast-then-filter pattern as every other handler. Wired into `IntegratedHandler` (new constructor parameter, dispatched after `factory`, before the agent-evaluation block) and `HeadlessHandler` (sim-cli, mirrored). Verified via Docker: 696 tests pass, Checkstyle clean. | — | — | — |
| ~~P1~~ **DONE** — Finance | ~~Expose Finance's ledger through a read-only, purpose-specific observation/projection.~~ | **Implemented 2026-08-20** alongside the module above. `FinanceObservation(BigDecimal cash, BigDecimal sales)` (colocated with `Ledger` in `sim-finance`, mirroring `AgentObservation`'s role for `SalesAgent`) with a static `from(Ledger)` factory. **Decision**: `SnapshotBuilder`/the API do **not** expose it yet — no current UI/API consumer needs financial data, and the task's own guidance was "don't add API surface speculatively." Revisit when a real consumer (a `FinanceAgent`, or a UI panel) actually needs it. | — | — | — |
| ~~P2~~ **DONE** — Finance | ~~Add tests for the financial invariants.~~ | **Implemented 2026-08-20.** `PostingTest` (rejects zero/negative amounts), `JournalEntryTest` (balanced entries construct; unbalanced entries throw with the correct debit/credit totals in the error; empty-postings rejected; multi-posting-per-side balancing works, since the invariant is sum-based not count-based), `LedgerTest` (zero balance with no postings; debit-normal vs. credit-normal balance direction; accumulation across entries; `entries()` immutability), `FinanceObservationTest`, `FinanceHandlerTest` (no posting before `OrderCompleted`, exactly one after; the exact `DR Cash 120.00 / CR Sales 120.00` shape for quantity=10 × unitPrice=12.0; ignores all other event types; each completed order posts its own entry). `ScenarioBaselinesTest.financeLedgerAgreesWithFactoryCompletedSalesValue` (Finance's `Cash` balance matches Factory's `completedSalesValue` under the current immediate-settlement policy — the two are expected to coincide today, per the earlier "reassessing CompletedSalesValue" note, but are checked, not assumed) and `financeLedgerIsReproducibleForSameScenarioAndSeed` (identical journal-entry sequence across two runs) cover the full-scenario invariants. "Factory cannot mutate Ledger and Finance cannot mutate Factory state" is enforced structurally, not by a runtime test: `sim-finance` depends only on `sim-types`/`sim-core` (no dependency on `sim-factory`), so it is not possible for `FinanceHandler` to call into `FactoryHandler` at all; a dedicated ArchUnit-style test remains P2 scope per the original backlog (see the existing "package-dependency test" item) and was not duplicated here. | — | — | — |
| P2 | Rename `AgentObservation.currentPrice` → `offerPrice` is already tracked above; additionally, once `sim-finance` exists, confirm no "revenue"-flavored naming has crept into Factory/Economy/Agents in the interim (`git grep -i revenue` outside `sim-finance`), and update the CONTRIBUTING.md guardrail 12 checklist item if any is found. | Cheap periodic hygiene check matching the new guardrail; catches drift between "Finance decided" and "Finance implemented." | Search + spot rename if needed. | Finance module landing | Low |

## D. Explicit non-goals (for this pass and near-term follow-up)

- **No generic event bus / pub-sub.** Arcogine's value is explicit, deterministic, easy-to-read execution order. A bus would trade that for indirection the task explicitly asked to avoid.
- **No CQRS framework or event-sourcing framework.** The existing `Event`/`EventLog`/`Scheduler` triad already gives replayability where it's needed; adopting a framework would add ceremony without a demonstrated need.
- **No global mutable `SimulationState` object.** That would recreate the duplicated-state problem at a larger scale instead of solving it.
- **No premature plugin framework** for future domains (inventory, procurement, finance, workforce, maintenance). The P2 state-query-interface pattern is scoped narrowly and should be validated against one real new domain before generalizing further.
- **No rewriting all handlers at once.** The backlog is staged specifically so `IntegratedHandler`, `FactoryHandler`, and `DemandModel` can be changed independently and reviewed in small, testable increments.
- **No fixing the Rust-era documentation staleness** (`CONTRIBUTING.md`'s crate/Cargo references, `docs/standards-alignment.md`'s "Crate" column) as part of this pass — real, but orthogonal to the Events–State–Observations philosophy this task scopes. Worth its own follow-up.
- **No implementation of the P0/P1/P2/P3 backlog items themselves** in this pass, beyond the documentation changes listed in section A — per the task's instruction to produce a staged backlog rather than execute the refactor. (The one exception remains the already-merged `SnapshotBuilder` fix from the original pass, whose semantics this update partially amends without re-touching the code.)
- **No sophisticated accounting model** (refined 2026-08-20 (2), was "no accounting/finance model"). A minimal Finance domain with a minimal double-entry ledger *is* the intentional current architecture, not a non-goal — see the "Finance established as a domain, now" update above. What remains explicitly out of scope: GAAP/IFRS compliance, configurable revenue-recognition frameworks, `AccountsReceivable`/`AccountsPayable` unless an actual scenario needs them, payment terms, tax, depreciation, multi-currency, debt/equity financing, inventory accounting, budgeting, forecasting, fiscal periods/closing, a generalized accounting rules engine, or ERP abstractions. `CompletedSalesValue` remains a Factory-owned operational/commercial KPI, distinct from (and not automatically identical to) whatever Finance eventually calls its Sales balance.
- **No generic event bus merely to accommodate Finance.** `OrderCompleted` is one more explicit event type dispatched through the same `Scheduler`/`IntegratedHandler` sequencing as everything else — Finance does not justify async dispatch or pub-sub any more than any other domain did.

## E. Suggested target architecture

```text
             Events
                |
                v
        Domain-owned State
                |
                v
     Read-only Observations
                |
                v
     Agents / Policies / Experiments
                |
                v
        Decisions / Commands
                |
                v
              Events
```

Concretely, for Arcogine as it exists today plus its near-term direction:

- `PricingState`, `FactoryHandler`, and future `InventoryHandler`/`FinanceHandler`/`WorkforceHandler` each own one slice of mutable state, mutated only inside their own `handleEvent`, with no cross-domain setters between them. `PricingState` owns `OfferPrice`; `FactoryHandler` owns each order's immutable `OrderPrice`/`OrderValue` (arriving on `OrderCreation`, never queried back from `PricingState`) and the derived `CompletedSalesValue`.
- Historical transaction facts travel *with the event that created them*, not through a later query against current state — `OrderCreation` carries the agreed `unitPrice`, so `FactoryHandler` never needs to ask "what's the price" after the fact; it already knows, from the event, what price applies to that specific order for its entire lifecycle. This is the concrete illustration of why event payloads should carry the facts a downstream domain needs, rather than requiring that domain to read another domain's live mutable state later.
- Cross-domain reads happen through narrow, read-only interfaces (the P2 state-query-interface pattern) or through purpose-specific `XObservation` records — never through a shared mutable field or a direct setter call from `IntegratedHandler`.
- `IntegratedHandler` stays the explicit, deterministic composition root — its job shrinks to *sequencing* handler calls in a documented order, not *synchronizing* their internals or *assembling* observations. Observation construction moves to small projector functions colocated with the observation type they build (following `AgentObservation`'s example).
- Agents and any future policy/experiment code only ever see Observations, and only ever produce Events/Decisions — `SalesAgent.decide()` is already the model to replicate. An agent can change `OfferPrice` for the future; it can never reach an existing order's `OrderPrice`.
- API/UI snapshots (`SnapshotBuilder`) remain a legitimate, separate projection for external consumers, but are documented as such and are never a second authority for a number (like `CompletedSalesValue`) that a domain observation also reports — the two must be demonstrably consistent, enforced by tests.
- Adding a new domain means: (1) a new `XHandler` implementing `EventHandler` that owns its own state, (2) a new `XObservation` record for whatever needs to read it, (3) one line in `IntegratedHandler`'s explicit sequence — not new setters scattered across existing handlers.
- Finance is the first concrete application of that pattern: `OrderCompleted` (an `EventPayload`, the operational fact) → `FinanceHandler` (owns `Ledger`, posts balanced `JournalEntry` records) → a `FinanceObservation` (read-only, purpose-specific) for whatever eventually needs to see cash/sales/receivables. Commercial truth (`OrderPrice`/`OrderValue`, fixed at `OrderCreation`), operational truth (`OrderCompleted`, fixed at fulfillment), and financial truth (the `JournalEntry` Finance derives from it) stay three distinct, separately-owned facts connected only by events — never by one domain reaching into another's state.

This preserves everything the constraints require — deterministic behavior, explicit ordering, module boundaries, headless-first design, testability, replayability — while removing the specific couplings this assessment found in `IntegratedHandler`, `DemandModel`, and `FactoryHandler`.
