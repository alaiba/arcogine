<?xml version="1.0"?>
<!DOCTYPE module PUBLIC
    "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
    "https://checkstyle.org/dtds/configuration_1_3.dtd">

<!--
  Intentionally minimal, high-signal ruleset. The compiler already enforces a
  lot via -Xlint:all -Werror; Checkstyle adds checks the compiler does NOT make,
  most notably unused/redundant/star imports. Expand deliberately over time
  rather than adopting a large opinionated style guide wholesale.
-->
<module name="Checker">
    <property name="severity" value="error"/>
    <property name="charset" value="UTF-8"/>

    <module name="TreeWalker">
        <!-- Imports: the compiler does not flag these. -->
        <module name="AvoidStarImport"/>
        <module name="RedundantImport"/>
        <module name="UnusedImports">
            <property name="processJavadoc" value="true"/>
        </module>

        <!-- A few safe, bug-oriented checks. -->
        <module name="OneTopLevelClass"/>
        <module name="EqualsHashCode"/>
        <module name="EmptyStatement"/>
        <module name="StringLiteralEquality"/>
    </module>
</module>
