# Adversarial research review handoff — Engine applicability to optional-record Factory policies

You are operating as an **independent Arcogine Researcher in adversarial-review mode**. Follow the current versions of:

- `AGENTS.md`
- `.github/agents/researcher.agent.md`
- `docs/development/researching.md`

Do not treat this prompt, the report, or the workspace branch as repository authority. The report is evidence under review.

## Exact report input

Review this exact completed research report revision:

- Repository: `alaiba/arcogine`
- Evidence workspace branch: `workspace/research-engine-v2-applicability`
- Report commit: `c409ecd0bb2941101907a7eb998d8d3ef6b7ce4a`
- Report path: `workspace/research/investigations/engine-applicability-optional-record-factory-policies.md`
- Report-stated research baseline: `7e5e6ad2f8a2707d00fff3e2a4e3db96da85852a`

Before substantive report-specific review, verify that the complete report can be resolved at that exact commit/path and that its stated baseline matches the report header. If it cannot, stop with:

`INPUT BLOCKED — ORIGINAL REPORT NOT AVAILABLE`

Do not infer the report from this prompt or from summaries.

## Independence requirement

This is a **high-risk** research conclusion. The adversarial review must be genuinely independent enough to satisfy the repository's high-risk research gate.

Use a fresh isolated session/run with no responsibility for defending the original conclusion; a different researcher/person/model family is preferable where practical. State explicitly in the review artifact how independence was achieved.

If you are not independent, do not claim an independent adversarial review. A self-administered pass does not satisfy the reconciliation prerequisite.

## Anchoring-control sequence

Do **not** deeply read or evaluate the report's recommendation first.

After confirming report identity/completeness only:

1. resolve live `main` and record its exact SHA as the review baseline;
2. read the current `AGENTS.md`, Researcher contract, and `docs/development/researching.md`;
3. read the bounded research question/brief from:
   `docs/research/investigations/engine-evolution.md#engine-applicability-to-optional-record-factory-policies`;
4. independently re-ground the relevant repository authorities;
5. independently reconstruct the major constraints;
6. independently identify plausible candidate answers;
7. independently derive likely proving and failure cases;
8. only then read the report's reasoning and recommendation in depth and attempt to falsify it.

This sequencing is mandatory for the value of the review.

## Current-state re-grounding

The report baseline was `7e5e6ad2f8a2707d00fff3e2a4e3db96da85852a`. Live `main` may have advanced.

Compare the report baseline to current live `main`. Inspect every changed path material to:

- research procedure or evidence custody;
- Engine semantic identity/support;
- Factory semantic evolution or `factory-model:v2`;
- publication/runtime assembly;
- spatial runtime consequences;
- compatibility/provenance/refusal semantics.

Do not assume a later commit is immaterial merely because its title sounds procedural.

For all current-state claims, fetch material files at the exact live-main review baseline.

## Authorities to inspect before reading the recommendation deeply

At minimum inspect the current versions of:

- `docs/research/research-register.md`
- `docs/research/investigations/engine-evolution.md`
- `docs/architecture/overview.md` — especially semantic evolution/support and determinism
- `docs/development/semantic-contract-support.md`
- `docs/architecture/factory-design.md` — especially Factory semantic evolution/applicability
- `docs/architecture/factory-model-v2.md`
- `docs/architecture/engine-semantics-v1.md`
- `docs/research/investigations/factory-model-semantic-composition.md`
- `docs/planning/spatial-runtime-consequences.md`
- `docs/planning/factory-simulation-engine-readiness.md`

Inspect executable evidence as needed, including the current Engine semantics identity, Factory publication/assembly seams, V2 model/validation, and Engine-v1 conformance/identity tests.

Search semantic neighbors rather than relying only on these named files.

## Independent reconstruction question

Without relying on the report's conclusion, answer:

> Which Engine semantics identity may execute artifacts of the reconciled `factory-model:v2` policy — required production records plus an optional complete spatial record — and can `engine-semantics:v1` be stated as applicable to them, with the spatial record present or absent, without changing its fixed definition?

Keep separate:

- Factory validity/publication;
- Engine semantic applicability;
- implemented execution support;
- result attribution/provenance;
- refusal of unsupported policy/content.

## Adversarial challenges to perform

Attempt to falsify the report's load-bearing conclusion using the repository-defined failure modes. In particular, test for:

### Omitted candidate

Look for a viable model the report failed to consider, including narrower support partitions, exact-policy versus content-sensitive applicability, or a correction/refusal interpretation that is semantically distinct from the report's candidate set.

Do not manufacture a candidate that has no coherent contract or consumer value.

### Preservation burden

For any claim that `engine-semantics:v1` can or cannot be corrected in place, reconstruct the whole-definition preservation test independently:

- accepted input domain;
- referenced Factory policy/content;
- result-affecting interpretation;
- outputs/observations/events;
- refusal/rejection behavior;
- provenance/identity meaning;
- historical interpretability.

Do not substitute current implementation coverage or existing test gaps for the fixed semantic definition.

### Spatial-absent case

Challenge whether a valid V2 artifact with no spatial record is:

- already semantically entailed as production-only execution;
- a newly admitted input case;
- necessarily unsupported by v1;
- representationally different but semantically equivalent in a way that matters or does not matter to Engine identity.

Test both directions. Do not assume policy-label difference alone proves Engine semantic difference; do not assume projection equivalence proves identity-preserving applicability.

### Spatial-present case

Challenge whether the reconciled optional-record change actually affects v1 applicability for a V2 artifact where the complete spatial record is present and the five spatial facts retain their meanings.

Determine whether a present-only correction can be proven, and whether that has any consequence for the both-form bounded question.

### Refusal semantics

Identify the exact unsupported cases that the fixed definition requires or permits the Engine to refuse.

Challenge any report inference that turns absence of an implemented pair selector into evidence about normative refusal semantics, or vice versa.

### Provenance and historical meaning

Test whether each candidate preserves the meaning of previously attributed `engine-semantics:v1` results.

Challenge any assumption that a different Factory fingerprint automatically changes Engine semantic identity, and any opposite assumption that equivalent production projections erase Factory provenance differences.

### Possibility versus necessity

Separate:

- what can be implemented safely;
- what the current fixed definition already entails;
- what would be a legitimate correction;
- what necessarily requires a new identity.

A feasible successor identity does not by itself prove that a successor is necessary. A feasible v1 implementation does not by itself prove identity-preserving applicability.

### Stale-baseline risk

Determine whether live-main changes since the report baseline materially weaken, strengthen, or invalidate any report claim.

## Proving cases

Derive your own set before consuming the report recommendation deeply. At minimum ensure the review tests:

- current Factory V1 under Engine v1;
- V2 with complete nonzero spatial record;
- V2 with present zero-valued spatial magnitudes;
- V2 with spatial record absent;
- invalid/incomplete spatial content;
- Factory-valid but Engine-unsupported policy/content;
- unsupported Engine identity;
- provenance when V1 and production-only V2 encode equivalent production meaning;
- a future Factory policy with optional content irrelevant to a given Engine interpretation;
- historical results already attributed to v1.

Add or remove cases when your independent reconstruction justifies it.

## Source discipline

Distinguish clearly:

- **Repository fact**
- **External evidence**
- **Inference**
- **Adversarial finding/qualification**

External evidence is optional and should be used only if it materially discriminates a candidate or exposes a failure mode not answerable from repository authority.

Do not use synthesis seeds as a framing premise for this independent review unless current research policy specifically requires it.

## Review the complete report

Only after the independent reconstruction above, read the exact report at commit `c409ecd0bb2941101907a7eb998d8d3ef6b7ce4a` in depth.

Challenge:

- source-to-claim accuracy;
- omitted evidence;
- candidate completeness;
- proving-case coverage;
- hidden implementation assumptions;
- identity/equality assumptions;
- stale baseline;
- possibility presented as necessity;
- conclusion strength versus evidence;
- qualifications that materially narrow the conclusion;
- whether any recommended durable consequence exceeds the bounded question.

A clean review is valid. Do not manufacture findings.

## Required disposition

Reach exactly one repository-defined adversarial-review disposition:

- `ACCEPT`
- `ACCEPT WITH QUALIFICATIONS`
- `MORE EVIDENCE REQUIRED`
- `REOPEN`

State:

- the independence condition;
- exact reviewed report commit/path;
- report-stated baseline;
- live-main review baseline;
- what was challenged;
- evidence considered;
- findings or qualifications;
- effect on the report's conclusion;
- whether the report is now decision-quality evidence for later architecture/specification reconciliation.

If `ACCEPT WITH QUALIFICATIONS`, state the qualifications precisely enough that a later reconciliation reviewer can verify they were carried forward.

## Persistence

Persist the completed adversarial-review artifact in the **same research-evidence workspace** when practical, without rewriting the handed-off report commit.

Use a semantic path under:

`workspace/research/investigations/`

For example:

`workspace/research/investigations/engine-applicability-optional-record-factory-policies-adversarial-review.md`

Commit the completed review and return only the active-custody handoff facts needed for the next phase:

- workspace branch;
- exact review commit SHA;
- review path;
- reviewed report commit SHA;
- live-main review baseline;
- disposition;
- independence statement.

Do not mark the research question `CONCLUDED` merely because the adversarial review is complete. Durable reconciliation remains a separate phase.
